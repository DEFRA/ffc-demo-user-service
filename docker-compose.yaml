version: '3.4'
services:
  ffc-demo-user-service:
    build: .
    image: ffc-demo-user-service
    container_name: ffc-demo-user-service${BUILD_NUMBER:-local}
    command: sh -c "scripts/wait-for/wait-for ffc-demo-user-postgres:5432 -- npm run migrate && npm run start:watch"
    depends_on:
      - ffc-demo-user-postgres
    environment:
      POSTGRES_DB: ffc_demo_users
      POSTGRES_PASSWORD: ppp
      POSTGRES_USERNAME: postgres
      POSTGRES_HOST: ffc-demo-user-postgres
      POSTGRES_PORT: 5432
    volumes:
      - node_modules:/usr/src/app/node_modules
      - .:/usr/src/app
    networks:
      - ffc-demo-users

  ffc-demo-user-postgres:
    container_name: ffc-demo-user-postgres${BUILD_NUMBER:-local}
    image: postgres:11.4-alpine
    environment:
      POSTGRES_DB: ffc_demo_users
      POSTGRES_PASSWORD: ppp
      POSTGRES_USERNAME: postgres
    networks:
      - ffc-demo-users

volumes:
  node_modules: {}

networks:
  ffc-demo-users: {}
